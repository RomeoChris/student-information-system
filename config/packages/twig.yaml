twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        brandName: '%brandName%'
        shortBrandName: '%shortBrandName%'
        footNote: '%footNote%'
